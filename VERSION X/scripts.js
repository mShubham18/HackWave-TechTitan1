document.addEventListener('DOMContentLoaded', function () {
    // Fetch and display recently added manga
    fetchAndDisplayRecentlyAddedManga();
});

function fetchAndDisplayRecentlyAddedManga() {
    // Fetch recently added manga data from server (you'll need to implement this)
    // Use AJAX, Fetch API, or any other method to fetch data from your backend
    // Example:
    // fetch('https://your-api.com/recently-added-manga')
    //     .then(response => response.json())
    //     .then(data => {
    //         // Process and display recently added manga data
    //         // Update the HTML content to display manga titles, authors, synopses, and cover images
    //     })
    //     .catch(error => console.error('Error fetching recently added manga:', error));
}
